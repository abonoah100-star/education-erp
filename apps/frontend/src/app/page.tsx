'use client';import {useEffect,useState} from 'react';import {useRouter} from 'next/navigation';import {request} from '@/lib/api';
export default function Login(){const router=useRouter();const[email,setEmail]=useState('admin@edu.local');const[password,setPassword]=useState('Admin@123');const[error,setError]=useState('');const[loading,setLoading]=useState(false);useEffect(()=>{if(localStorage.getItem('accessToken'))router.replace('/workspace')},[router]);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');try{const data=await request<{accessToken:string;refreshToken:string}>('/auth/login',{method:'POST',body:JSON.stringify({email,password})});localStorage.setItem('accessToken',data.accessToken);localStorage.setItem('refreshToken',data.refreshToken);router.push('/workspace');}catch(err){setError(err instanceof Error?err.message:'تعذر تسجيل الدخول')}finally{setLoading(false)}}return <main className="login"><section className="login-visual"><div><div className="brand">EduCore ERP</div><h1>مساحة تشغيل تعليمية<br/>مصممة للإدارة الفعلية.</h1><p>فروع، خزائن، صلاحيات ومتابعة تشغيلية في نظام واحد قابل للتوسع.</p></div><small>Foundation Sprint 1 · Modular Monolith</small></section><section className="login-form"><form className="form-box" onSubmit={submit}><p className="muted">تسجيل الدخول إلى بيئة التجربة</p><h2>مرحبًا بعودتك</h2><label className="field">البريد الإلكتروني<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></label><label className="field">كلمة المرور<input value={password} onChange={e=>setPassword(e.target.value)} type="password" required/></label><button className="primary" disabled={loading}>{loading?'جارٍ الدخول...':'دخول النظام'}</button>{error&&<p className="error">{error}</p>}</form></section></main>}
